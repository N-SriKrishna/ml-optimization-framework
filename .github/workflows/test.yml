name: Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.10']

    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        # Install conflicting packages WITHOUT dependency checking
        pip install --no-deps tf2onnx==1.16.1
        pip install --no-deps paddleseg==2.8.0
        # Install everything else from requirements.txt
        pip install -r requirements.txt
        # Ensure pytest tools are installed
        pip install pytest pytest-cov
    
    - name: Run tests
      run: |
        PYTHONPATH=. pytest tests/ -v --cov=src --cov-report=xml --cov-report=term
    
    - name: Upload coverage
      uses: codecov/codecov-action@v3
      with:
        files: ./coverage.xml
        flags: unittests
        name: codecov-umbrella
